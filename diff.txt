SELECT 
  COALESCE(t1.id, t2.id) AS id,
  t1.target_column AS table1_value,
  t2.target_column AS table2_value
FROM `project.dataset.table1` t1
FULL OUTER JOIN `project.dataset.table2` t2
  ON t1.id = t2.id
WHERE t1.target_column IS DISTINCT FROM t2.target_column
